export default function BacklogPage({ params }: { params: { orgSlug: string } }) {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-6 text-foreground">Backlog</h1>
      {/* Backlog list goes here */}
    </div>
  );
}
